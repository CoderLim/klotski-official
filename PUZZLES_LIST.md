# 华容道棋局列表

本项目共包含 **40 个经典华容道棋局**，难度从简单到专家。

## 难度分布

- 🟢 **简单 (Easy)**: 2 个棋局
- 🟡 **中等 (Medium)**: 12 个棋局
- 🟠 **困难 (Hard)**: 20 个棋局
- 🔴 **专家 (Expert)**: 6 个棋局

## 棋局列表

### 简单难度 (2)

1. **捷足先登** (`jiezuxiandeng`)
2. **近在咫尺** (`jinzaichichi`)

### 中等难度 (12)

3. **齐头并进** (`qitoubingjin`)
4. **雨声淅沥** (`yushengxili`)
5. **左右布兵** (`zuoyoububing`)
6. **一路进军** (`yilujinjun`)
7. **一路顺风** (`yilushunfeng`)
8. **围而不歼** (`weierbujian`)
9. **插翅难飞** (`chachinanfei`)
10. **双将挡路** (`shuangjiangdanglu`)
11. **小燕出巢** (`xiaoyanchuchao`)
12. **屯兵东路** (`tunbingdonglu`)

### 困难难度 (20)

13. **横刀立马** (`hengdao`)
14. **指挥若定** (`zhihuiruoding`)
15. **将拥曹营** (`jiangyongcaoying`)
16. **并分三路** (`bingfensanlu`)
17. **桃花园中** (`taohuayuanzhong`)
18. **守口如瓶** (`shoukouruping`)
19. **守口如瓶 (变体)** (`shoukouruping-2`)
20. **横马当关** (`hengmadangguan`)
21. **层层设防** (`cengcengshefang`)
22. **层层设防 (变体)** (`cengcengshefang-2`)
23. **兵挡将阻** (`bingdangjiangzu`)
24. **堵塞要道** (`dusaiyaodao`)
25. **四面楚歌** (`simianchuge`)
26. **前呼后拥** (`qianhuhouyong`)
27. **兵临曹营** (`binglincaoying`)
28. **五将逼宫** (`wujiangbigong`)
29. **前挡后阻** (`qiandanghouzu`)
30. **走投无路** (`zoutouwulu`)
31. **比翼横空** (`biyihengkong`)
32. **夹道藏兵** (`jiadaocangbing`)
33. **四将连关** (`sijianglianguan`)
34. **峰回路转** (`fenghuiluzhuan`)

### 专家难度 (6)

35. **瓮中之鳖** (`wengzhongzhibie`)
36. **层峦叠嶂** (`cengluandiezhang`)
37. **水泄不通** (`shuixiebutong`)
38. **四路进兵** (`silujinbing`)
39. **入地无门** (`rudiwumen`)
40. **勇闯五关** (`yongchuangwuguan`)

## 访问方式

每个棋局都可以通过以下 URL 格式访问：
```
http://localhost:3000/p/{slug}
```

例如：
- 横刀立马: `http://localhost:3000/p/hengdao`
- 捷足先登: `http://localhost:3000/p/jiezuxiandeng`
- 勇闯五关: `http://localhost:3000/p/yongchuangwuguan`

## 技术细节

所有棋局配置位于 `lib/puzzles/index.ts` 文件中，每个棋局包含：

- **name**: 棋局中文名称
- **slug**: URL 友好的唯一标识符
- **difficulty**: 难度等级 (easy/medium/hard/expert)
- **blocks**: 方块配置数组，每个方块包含：
  - `shape`: [高度, 宽度] - 方块尺寸
  - `position`: [行, 列] - 方块在棋盘上的位置（0-indexed）

## 方块类型

- **2×2 方块**: 曹操（目标方块，红色）
- **2×1 竖将**: 垂直的 2 格高方块（蓝色）
- **1×2 横将**: 水平的 2 格宽方块（绿色）
- **1×1 小兵**: 单格方块（灰色）

## 游戏目标

将 2×2 的曹操方块移动到棋盘底部中央的出口位置（第 4 行，第 2-3 列）。

